# -*- coding: utf-8 -*-
"""Resume scanner.ipynb

Automatically generated by Colaboratory.

Original file is located at
    https://colab.research.google.com/drive/1QogkhWxeprcQrdyyW8TTbJlcrpiCuboH
"""

#A program to see how similar a resume is to a job description

#instal lthe docx2txt package
pip install docx2txt

#Import the library
import docx2txt

#Load the data
from google.colab import files
uploaded = files.upload()

#Store the resume
resume = docx2txt.process('python_resume.docx')

#print the resume
print(resume)

#Store the job description into a variable, might need to be adapted depending on the previous results
job_description = docx2txt.process('job_description.docx')

#Print the job description
print(job_description)

#A list of text
text = [resume, job_description]

from sklearn.feature_extraction.text import CountVectorizer
cv = CountVectorizer()
count_matrix = cv.fit_transform(text)

from sklearn.metrics.pairwise import cosine_similarity

#Print the similarity scores
print('\Similarity Scores:')
print(cosine_similarity(count_matrix))
#Show how similar the two documents are

#Get the match percentage
matchPercentage = cosine_similarity(count_matrix)[0][1]*100 #get that point XY from last print value and multiply it by 100
matchPercentage = round(matchPercentage, 2) #round to two decimal places
print('Your resume machtes about '+ str(matchPercentage)+ '% of the job description.')
